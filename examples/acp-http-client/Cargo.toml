[package]
name = "acp-http-client"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
remi-agentloop-transport = { path = "../../remi-agentloop-transport", features = ["http-client"] }
tokio = { version = "1", features = ["full"] }
futures = "0.3"
serde_json = "1"
